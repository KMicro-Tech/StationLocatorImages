image: node:latest

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp *.json .public/  # Copy JSON files to the public directory
  artifacts:
    paths:
      - .public
  tags:
    - clean-energy  # Specify the runner tag here
